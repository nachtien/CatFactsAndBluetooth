CREATE TABLE IF NOT EXISTS Fact(
  id INTEGER NOT NULL PRIMARY KEY,
  message TEXT NOT NULL
);

insertItem:
INSERT OR REPLACE INTO Fact(message, id) VALUES ?;

selectAll:
SELECT * FROM Fact;

deleteAll:
DELETE FROM Fact;

lastId:
SELECT id FROM Fact ORDER BY id DESC;
